services:
  enchanter:
    build: .
    container_name: enchanter
    ports:
      # 既定では自PCからのみアクセス可能。LAN内の他端末からもアクセスしたい場合は
      # "8787:8787" に変更してください(コンテナ内部は常に0.0.0.0で待ち受けています)
      - "127.0.0.1:8787:8787"
    volumes:
      - ./data:/data
    restart: unless-stopped
